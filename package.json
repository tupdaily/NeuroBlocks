{
  "name": "ai-playground",
  "version": "1.0.0",
  "description": "Scratch-like model builder – plug and play layers to design trainable models",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=ai-playground-frontend",
    "build": "npm run build --workspace=ai-playground-frontend",
    "preview": "npm run preview --workspace=ai-playground-frontend"
  }
}
